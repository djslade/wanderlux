export const mockAuthService = {
  signup: jest.fn(),
  login: jest.fn(),
  hashPassword: jest.fn(),
  createUser: jest.fn(),
  getUserData: jest.fn(),
  validatePassword: jest.fn(),
  signAccessToken: jest.fn(),
};
